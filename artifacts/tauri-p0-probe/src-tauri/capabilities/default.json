{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Probe capability: the drag region needs an explicit start-dragging permission",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
